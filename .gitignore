# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# uv / venv (uv.lock IS committed for reproducibility)
.venv/

# pytest / tooling caches
.pytest_cache/
.ruff_cache/
openspec/

# model / HF caches (never vendor weights into the repo)
*.safetensors
*.bin
*.pt
*.pth
*.ckpt
.cache/
checkpoints/
exports/
runs/
data/
datasets/
wandb/

# Modal / W&B / local service logs
.modal/
modal-logs/
modal_logs/

# raw benchmark run artifacts (the curated result is folded into docs/benchmark.md)
bench-results/
benchmark-payloads/
raw-data/
outputs/
artifacts/
traces/
models/
model-weights/

# local logs / database-like scratch outputs
*.log
*.sqlite
*.db
*.parquet
*.arrow

# OS
.DS_Store
.claude/

# maintainer-local engineering notes (roadmap, phase journals, lab evidence)
docs/internal/
